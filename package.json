{
  "name": "escolares",
  "version": "1.0.0",
  "description": "Nada interesante por acá ahora.",
  "main": "index.js",
  "scripts": {
    "deploy": "browserify app.js > public/bundle.js && cp -Rf node_modules/leaflet/dist public/leaflet && divshot deploy",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/bluelemons/escolares.git"
  },
  "author": "",
  "license": "AGPL-3.0+",
  "bugs": {
    "url": "https://github.com/bluelemons/escolares/issues"
  },
  "homepage": "https://github.com/bluelemons/escolares#readme",
  "dependencies": {
    "browserify": "^11.0.1",
    "ecstatic": "^0.8.0",
    "leaflet": "^0.7.5"
  },
  "devDependencies": {
    "divshot-cli": "^1.10.5"
  }
}
